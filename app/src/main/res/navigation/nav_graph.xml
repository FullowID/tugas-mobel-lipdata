<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/switchLayoutFragment">

    <fragment
        android:id="@+id/switchLayoutFragment"
        android:name="com.example.automotiveshandbook.ui.SwitchLayoutFragment"
        android:label="@string/app_name">
        <action
            android:id="@+id/action_switchLayoutFragment_to_detailActivity"
            app:destination="@id/detailActivity" />
    </fragment>
    <activity
        android:id="@+id/detailActivity"
        android:name="com.example.automotiveshandbook.DetailActivity"
        android:label="Detail"
        tools:layout="@layout/activity_detail" />
</navigation>